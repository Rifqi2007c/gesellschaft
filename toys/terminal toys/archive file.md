### zip
- package name
	- `zip`
- archive file into zip
	- `zip -r <filename.zip> <folder-location>`
		- `<filename.zip>` is the name of the file, put name for the zip file then the folder location

----
#toys  
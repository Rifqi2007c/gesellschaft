CPU intensive hash cracker

### simple usage
```
john --format=raw-sha256 hash.txt
```
- `--format=<format>` crack base on the hash format
	- `--list=formats` to list all the format available
- the hash code is required to be put in side a .txt file. put it directly will not work

___
#cybersecurity 